codeship_pipeline:
  build:
    image: hubtmp.byu.edu/codeship/pythonbase
    dockerfile_path: Dockerfile-Codeship
    encrypted_dockercfg_path: dockercfg.encrypted
  encrypted_env_file: secrets.env.encrypted
  add_docker: true
  cached: false
